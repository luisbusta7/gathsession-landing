{
  "name": "sass-vite-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy":" npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "sass": "^1.99.0",
    "vite": "^6.1.0"
  },
  "dependencies": {
    "D": "^1.0.0",
    "gh-pages": "^6.3.0"
  }
}
